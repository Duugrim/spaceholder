Логично предположить, что формула урона должна содержать сумму компонентов, некоторые из которых могут обращаться в ноль в зависимости от обстоятельств.

- Дистанция никогда не может быть равна нулю. 
- Скорость не совсем "равна нулю", но компонент становится нулём когда "стойка" больше нуля. 0 = стоит, 1 = двигается, 2 = уклоняется.
- Укрытие может быть равно нулю, если его нет (разумеется). Если оно есть, то при = 1 попадание должно быть почти невозможно. Надо его как-то связать с размером цели.
- Скилл может быть равен нулю, но не уверен, с чем он должен быть связан.
- Точность (стат) вполне может быть связан с аим (статусом), ведь способность прицелится вполне может быть связана с физическим талантом.
- Положение стрелка - МОЖЕТ ВЛИЯТЬ НЕ НА САМУ ФОРМУЛУ, А НА СКОРОСТЬ ГЕЙНА АИМА
- Попадание если результат больше или равен 1, промах если меньше или равно -1, если между - задел, меньше урона и бронепробития
- Дробовики должны иметь преимущество для попадания (ну, в целом штуки, которые бьют дробью)
- Скорострельность должна быть преимуществом как в уроне в секунду, так и в попытках попасть в слабую точку. Но отдачу должно быть тяжело контролировать - может быть, 
# Формула
`Дистанция` / `Дальность оружия` - `Скорость цели` х `Положение цели` + `Размер` х (1 - `Укрытие`)



\+ размер цели = 1 (стандартный размер) на укрытие (что по торчит) на угловой размер, наверное?
\+ навык (0-10)
\+ прицеливание (0-10-?)
\+ точность (~1)
\- движение цели (~3)
\- уклонение цели

(Skill + Aim) x Stat_acc
(0-10 + 0-10) x ~1
~3

УГЛОВОЙ РАЗМЕР В ПЛЮС
УГЛОВАЯ СКОРОСТЬ В МИНУС
```
((skill+aiming)*acc)
+ t_size*t_cover 
-
(1 + isLaser)/2 как одно из влияний дистанции к скорости
```
