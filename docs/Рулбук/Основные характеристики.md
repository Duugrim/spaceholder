Мускулы
Выносливость
Подвижность
Координация
Сообразительность
Восприятие
Удача

При создании персонажа сначала выбираются сила, интеллект, и настойчивость, а потом генерятся статы выше

1. Как менять переменную предмета в айтем контейнере?
2. Есть динамическая таблица, и мне нужно достать оттуда значение, соответствующее фильтру в той же строке. Например, есть таблица `ammo_damage` со столбцами `type` и `damage`, и мне нужно найти строку у которой `type`=`melee`, и если такая строка есть - получить значение из той же строки, но столбца `damage`.